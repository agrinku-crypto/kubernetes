apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"  # <--- Add this line
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: my-app.test
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-web-service # Must match your Service name
            port:
              number: 80
